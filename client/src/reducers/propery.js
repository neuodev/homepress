import {gethome} from '../actions/actionsType'
export const homeProperty = (state = {} , {type, payload} ) => {
    switch (type) {
        case GET_HOME_PROPERTY_REQUEST:
            
            break;
    
        default:
            break;
    }
}